/** The translation the reader shows. The DB can hold more; the app ships the KJV only. */
export const TRANSLATION = "KJV";

/** What the translation is called where it is named in full, as on a shared verse card. */
export const TRANSLATION_NAME = "King James Version";
